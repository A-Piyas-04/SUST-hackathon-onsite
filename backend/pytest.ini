[pytest]
# Run from backend/ so `import app...` resolves. Member 2 (P1-M2) tests live
# under tests/coordination/. Member 1/3 add their own suites alongside later.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
