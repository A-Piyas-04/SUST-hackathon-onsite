Owned by Member 2.

Superseded by the real Member 2 package at `app/coordination/` (P1-M2
scaffolding onward). This placeholder is retained only so the historical
import path resolves; new work goes under `app/coordination/`.

Route composition entry point for Member 1:
`app.coordination.router.include_member2_routers(app)`.
